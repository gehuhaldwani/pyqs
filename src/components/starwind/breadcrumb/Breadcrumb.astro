---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"nav">;

const { class: className, ...rest } = Astro.props;
---

<nav data-sw-breadcrumb aria-label="breadcrumb" class={className} {...rest} data-slot="breadcrumb">
  <slot />
</nav>
